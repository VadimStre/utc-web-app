# Изначальная задача

> Записано 2026-09-23. Текст задачи не редактируется — он эталон, с которым
> сверяется готовый результат. Всё, что сказано позже, дописывается
> в «Дополнения» ниже.

> «Применяя Autopilot
> Разверни проект utc-web-app на Render.com: подключи GitHub-репозиторий, создай PostgreSQL, настрой переменные окружения, выполни миграцию БД и сид, деплой, проверь доступность по HTTPS
> Предварительная работа была выполнена в предыдущем сеансе:
> Важно  «паспорт проекта»:
>
> Репозиторий: https://github.com/VadimStre/utc-web-app (public, ветка main), коммит 241c2d1 — Render-ready: tailwind/postcss/autoprefixer в dependencies, lock синхронизирован, диагностические скрипты удалены.
> Render: Web Service из этого репозитория; Build: npm install && npm run build; Start: npm run start; PostgreSQL (Internal URL).
> Переменные окружения (значения — из .env локально, НЕ в чат): DATABASE_URL (из Render Postgres), NEXTAUTH_SECRET, NEXTAUTH_URL=https://<имя-servisa>.onrender.com, ABACUS_API_KEY, ABACUS_API_BASE_URL=https://routellm.abacus.ai/v1, ABACUS_MODEL=route-llm, SERPER_API_KEY.
> После деплоя: при первом запуске prisma db push + сид автоматически (entrypoint есть? на Render npm run start без entrypoint — db push/seed надо сделать вручную или через команду деплоя; отметьте это в новом сеансе: выполнить npx prisma db push && npx tsx scripts/seed-if-empty.ts перед стартом).
> Вход: admin@utc.local / [REDACTED:ADMIN_SEED_PASSWORD].»

## Дополнения

- 2026-09-23 — «Нет аккаунта — зарегистрируюсь сейчас через GitHub» (ответ: аккаунт Render ещё не создан; регистрируется через GitHub).
- 2026-09-23 — «Готово, вошёл в панель» (аккаунт Render создан, пользователь в панели).
- 2026-09-23 — «В этом окне не пускает, возможно нужен VPN из России. Я открыл в Chrome через VPN» (с этой машины api.render.com режется; вход в Render через VPN в браузере).
- 2026-09-23 — «1» (выбран ручной путь через веб-панель в Chrome; оркестратор даёт шаги, пользователь кликает).